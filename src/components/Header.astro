---
interface Props {
  currentPage?: string;
}

const { currentPage = '' } = Astro.props;

const navItems = [
  { label: 'Home', href: '/' },
  { label: 'Learn', href: '/learn' },
  { label: 'Campaign', href: '/campaign' },
  { label: 'Blog', href: '/blog' },
  { label: 'Organize', href: '/organize' },
  { label: 'Resources', href: '/resources' },
  { label: 'Sign Petition', href: '/sign', highlight: true }
];
---

<header class="border-b-4 border-am-dark bg-am-blue sticky top-0 z-50">
  <nav class="max-w-7xl mx-auto px-4 py-4 flex items-center justify-between" aria-label="Main navigation">
    {/* Logo */}
    <a href="/" class="text-2xl md:text-3xl font-bold text-am-dark no-underline" aria-label="Public Code US - Home">
      PUBLIC<br />CODE US
    </a>

    {/* Desktop Navigation */}
    <div class="hidden md:flex gap-1" role="list">
      {navItems.map((item) => (
        <a
          href={item.href}
          role="listitem"
          aria-current={currentPage === item.href ? 'page' : undefined}
          class={`px-4 py-2 border-2 border-am-dark uppercase font-black text-sm tracking-widest transition-all no-underline focus-visible:outline-3 focus-visible:outline-am-dark focus-visible:outline-offset-2 ${
            item.highlight
              ? 'bg-am-red text-am-dark shadow-am hover:shadow-am-lg'
              : 'bg-am-light text-am-dark hover:shadow-am'
          } ${currentPage === item.href ? 'bg-am-light-blue text-am-dark' : ''}`}
        >
          {item.label}
        </a>
      ))}
    </div>

    {/* Mobile Menu Button */}
    <button
      id="mobile-menu-btn"
      class="md:hidden border-2 border-am-dark p-2 bg-am-light hover:shadow-am focus-visible:outline-3 focus-visible:outline-am-dark focus-visible:outline-offset-2"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="Open navigation menu"
    >
      <span class="text-am-dark font-bold" aria-hidden="true">MENU</span>
    </button>
  </nav>

  {/* Mobile Navigation */}
  <nav id="mobile-menu" class="hidden md:hidden border-t-4 border-am-dark bg-am-light" aria-label="Mobile navigation">
    {navItems.map((item) => (
      <a
        href={item.href}
        aria-current={currentPage === item.href ? 'page' : undefined}
        class={`block px-4 py-3 border-b-2 border-am-dark uppercase font-black text-sm tracking-widest no-underline focus-visible:outline-3 focus-visible:outline-am-dark focus-visible:outline-offset-[-3px] ${
          item.highlight
            ? 'bg-am-red text-am-dark'
            : 'bg-am-light text-am-dark hover:bg-am-blue'
        } ${currentPage === item.href ? 'bg-am-light-blue text-am-dark' : ''}`}
      >
        {item.label}
      </a>
    ))}
  </nav>
</header>

<script>
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  if (mobileMenuBtn && mobileMenu) {
    mobileMenuBtn.addEventListener('click', () => {
      const isExpanded = mobileMenuBtn.getAttribute('aria-expanded') === 'true';
      mobileMenuBtn.setAttribute('aria-expanded', String(!isExpanded));
      mobileMenuBtn.setAttribute('aria-label', isExpanded ? 'Open navigation menu' : 'Close navigation menu');
      mobileMenu.classList.toggle('hidden');
    });

    // Close menu when a link is clicked
    const links = mobileMenu.querySelectorAll('a');
    links.forEach((link) => {
      link.addEventListener('click', () => {
        mobileMenu.classList.add('hidden');
        mobileMenuBtn.setAttribute('aria-expanded', 'false');
        mobileMenuBtn.setAttribute('aria-label', 'Open navigation menu');
      });
    });

    // Close menu on Escape key and return focus to trigger
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && !mobileMenu.classList.contains('hidden')) {
        mobileMenu.classList.add('hidden');
        mobileMenuBtn.setAttribute('aria-expanded', 'false');
        mobileMenuBtn.setAttribute('aria-label', 'Open navigation menu');
        mobileMenuBtn.focus();
      }
    });
  }
</script>
